#############################
# 1단계: 빌드 단계 (Build Stage)
#############################
FROM openjdk:21-slim AS build

# 작업 디렉토리 설정
WORKDIR /app

# Gradle Wrapper 및 관련 파일 복사
COPY gradlew .
COPY gradle/ gradle/

# Gradle 빌드 스크립트 복사
COPY build.gradle .
COPY settings.gradle .

# 소스 코드 복사
COPY src/ src/

# Gradle Wrapper 실행 권한 부여
RUN chmod +x gradlew

# Gradle 빌드 (테스트 제외, --no-daemon 옵션 사용)
RUN ./gradlew build -x test --no-daemon

#############################
# 2단계: 실행 단계 (Runtime Stage)
#############################
FROM openjdk:21-slim

# 작업 디렉토리 설정
WORKDIR /app

# 애플리케이션이 사용할 포트 노출 (여기서는 8080)
EXPOSE 8080

# 빌드 단계에서 생성된 JAR 파일 경로를 ARG로 지정 (필요 시 경로를 수정하세요)
ARG JAR_FILE=/app/build/libs/planit.jar

# 빌드 단계에서 생성된 JAR 파일을 app.jar로 복사
COPY --from=build ${JAR_FILE} app.jar

# 컨테이너 시작 시 ENTRYPOINT를 이용하여 docker 프로파일로 실행
ENTRYPOINT ["java", "-Dspring.profiles.active=docker", "-jar", "app.jar"]
