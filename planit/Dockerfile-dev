# Dockerfile-dev

# 1. 베이스 이미지 설정 (Java 21 사용)
FROM openjdk:21-jdk-slim

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. 프로젝트 파일 복사
COPY planit/gradlew planit/gradle /app/planit/
COPY planit/build.gradle planit/settings.gradle /app/planit/
COPY planit/src /app/planit/src

# 4. Gradle 빌드 및 애플리케이션 빌드
RUN apt-get update && apt-get install -y bash
RUN chmod +x planit/gradlew
RUN ./planit/gradlew build -x test --no-daemon

# 5. 애플리케이션 실행 (라이브 리로드 등 개발용 설정 포함 가능)
CMD ["java", "-jar", "planit/build/libs/planit.jar"]
